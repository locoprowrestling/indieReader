# Disabled Workflows

These content-generation workflows were disabled during the indieReader sunset
on Monday, July 6th, 2026.

They remain here for audit history, but GitHub only runs workflow files from
`.github/workflows/`.

The GitHub Pages deploy workflow remains active in `.github/workflows/` so
future static page updates can be published without restoring the fetch,
generate, or tweet automations.
