name: Generate Evening Post

on:
  schedule:
    - cron: "0 1,2 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Check Denver evening window
        id: schedule
        run: |
          HOUR="$(TZ=America/Denver date +%H)"
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" || "$HOUR" == "19" || "$HOUR" == "20" ]]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/checkout@v4
        if: steps.schedule.outputs.should_run == 'true'
      - uses: actions/setup-node@v4
        if: steps.schedule.outputs.should_run == 'true'
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
        if: steps.schedule.outputs.should_run == 'true'
      - name: Generate evening blog post or carry over state
        if: steps.schedule.outputs.should_run == 'true'
        run: node scripts/run-generate.js evening
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ vars.OPENAI_MODEL }}
      - name: Commit post or carry-over state
        if: steps.schedule.outputs.should_run == 'true'
        run: |
          git config user.name "indieReader Bot"
          git config user.email "noreply@indiereader"
          git add src/content/posts/ data/state.json
          git diff --staged --quiet || (git commit -m "feat: evening post or carry-over $(date -u +%Y-%m-%d)" && git push)
